git add . #agrega los archivos para ser comiteados

git commit -m "mi mensaje" #genera un commit con los archivos que estaban preparados (Stage)

git push origin main #envia el commit al repositorio remoto (github)

git status
